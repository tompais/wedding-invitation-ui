npx @biomejs/biome check --write .
STAGED_TSX=$(git diff --cached --name-only --diff-filter=ACM | grep -E '\.(jsx|tsx)$' || true)
if [ -n "$STAGED_TSX" ]; then
  echo "$STAGED_TSX" | xargs rustywind --write
fi
